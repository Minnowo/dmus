



const Duration veryFastSnackBarDuration = Duration(milliseconds: 100);
const Duration fastSnackBarDuration = Duration(milliseconds: 500);
const Duration mediumSnackBarDuration = Duration(milliseconds: 1000);
const Duration longSnackBarDuration = Duration(milliseconds: 2500);

